from classes.agent_source.agent import Agent
from classes.simple_objects.position import Position
from classes.simple_objects.shape import Shape


# can implement if it is decided that using high level pathfinding algorithm to decide paths is much slower

def pathfinder(start_position: Position, goal_position: Position, shape: Shape, environment):
    return environment
